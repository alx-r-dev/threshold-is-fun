- state is initially initialVdotLocalValue
- state will be used like state.formattedMile
- on button submit is when we call the action with will be the form submission
  that updates our localstorage state
